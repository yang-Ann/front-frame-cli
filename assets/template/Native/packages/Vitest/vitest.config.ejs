import { defineConfig, configDefaults, defaultExclude } from "vitest/config";
#BR#
// https://cn.vitest.dev
export default defineConfig({
  test: {
    ...configDefaults,
    exclude: [
      ...defaultExclude,
      // ...
    ]
  },
}); 